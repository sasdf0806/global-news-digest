# 参与贡献

感谢参与 Global News Digest。提交代码前请先阅读 [AGENTS.md](./AGENTS.md) 和 [PROJECT_SPEC.md](./PROJECT_SPEC.md)。

1. 从 `main` 创建功能分支，分支名建议使用 `codex/<topic>`。
2. 从 `config/.env.example` 创建本地环境变量文件，禁止提交密钥。
3. 保持代码、接口、数据库迁移和文档同步更新。
4. 为核心逻辑补充测试，并在提交前运行格式化、静态检查和测试。
5. 提交信息使用 `feat:`、`fix:`、`docs:`、`test:` 等约定前缀。

Pull Request 请说明变更目的、测试方式、配置或迁移影响，以及是否涉及新闻来源许可或版权处理。
