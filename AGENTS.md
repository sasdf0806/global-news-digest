# 开发协作规范

## 基本原则

- 所有代码、接口和数据库变更必须同步更新文档。
- 不在代码中写入 API Key、Webhook、数据库密码或其他密钥。
- 任何新闻内容都必须保留来源链接。
- 事实摘要和模型分析禁止混用。
- 采集、AI 生成和日报发布任务必须支持重试与幂等。

## 代码规范

- 前端使用 TypeScript 严格模式。
- Python 使用 Ruff、Black 和类型标注。
- API 使用 REST 风格，统一响应格式见 `docs/api.md`。
- 数据库变更必须使用迁移脚本，不直接修改生产数据库。
- 业务逻辑放在服务层，路由层只做参数校验和调用。

## 测试要求

- 每个核心模块至少有单元测试。
- 新闻去重、事件聚类和热点排序必须有固定样例测试。
- API 必须包含成功、参数错误和权限错误测试。
- 日报生成必须有快照或结构化断言测试。

## 提交规范

提交信息使用以下格式：

```text
feat: 新增功能
fix: 修复问题
refactor: 重构
docs: 文档更新
test: 测试更新
chore: 工程配置
```

## 本地开发约定

- 从 `config/.env.example` 复制生成本地环境变量文件。
- 本地默认使用开发数据库，不连接生产数据库。
- AI 调用、新闻采集和定时任务应支持 Mock 模式。
- 提交前运行格式化、静态检查和测试。
